# Official github.com/supabase/auth (GoTrue). Prefer ECR public mirror for build.
# Tag aligns with lowcode-platform AUTH_IMAGE_TAG default (v2.195.0).
FROM public.ecr.aws/supabase/gotrue:v2.195.0

ENV TZ=Asia/Shanghai
